<p id="notice"><%= notice %></p>

<h1>Listing Items</h1>

<table class="table">
  <thead>
    <tr>
      <th>Description</th>
      <th>Quantity</th>
      <th>Price</th>
      <th colspan="1"></th>
    </tr>
  </thead>
  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= item.description %> </td>
        <td><%= item.quantity %></td>
        <td>£<%= item.price %></td>
        <td> <button>Get one !!</button> </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
